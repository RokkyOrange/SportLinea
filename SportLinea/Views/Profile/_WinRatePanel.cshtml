@model double

<div class="card-panel win-rate-panel">
    <p class="mb-0 win-rate-panel__text">
        Процент побед:
        <strong class="win-rate-panel__value">@Model.ToString("F1")%</strong>
    </p>
</div>
